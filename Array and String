Array的属性和方法：
属性：constructor：返回对创建此对象的数组函数的引用
      Length：设置或者返回数组中元素的数目
	  Prototype：是可以向对象添加属性和方法。
方法： concat（）===连接两个或更多数组，并返回结果。
		Join（）===把数组所有元素放入一个字符串，元素通过指定分隔符进行分割；
		Pop（）===删除并返回数组最后一个元素
		Push（）===向数组末尾添加一个或者更多元素，返回新的长度
		Reverse（）===点到数组中元素的顺序
		Shift（）===删除并返回数组的第一个元素
		Unshift（）===向数组开头添加一个或更多元素，并返回新的长度
		Slice（）===向某个已有的数组返回选定的元素
		Sort（）===对数组元素进行排序
		Splice（）===删除元素，并向数组添加新元素。接受三个参数，第一个参数表示
		起始位置，要删除项数和要插入的项（若无则表示只删除，不插入）
		Tosource（）返回该对象的源代码
		Tostring（）将数组转换成字符串，并返回结果
		Tolocalstring（）将数组转换成本地字符串，并返回结果
		Valueof()返回数组对象的原始值。

String数组的属性和方法：
属性：length：表示字符串中包含多少个字符
方法：
（一）字符方法：
		1.charAt()===返回给定位置的那个字符
		2.charCodeAt()===返回给定位置的字符编码
		这两个方法都接收一个参数，即基于0的字符位置
（在支持浏览器中，可以使用方括号加数字索引来访问字符串中的特定字符）
（二）字符串操作方法：
  1.	concat()===用于将一或者多个字符串拼接起来，返回拼接得到的新字符串。
（2-4方法都是基于字符串创建新字符串的方法，返回被操作字符串的子字符串，第一个参数指定字符串的开始位置，第二个参数表示字符串到哪里结束）
  2.	slice()===第二个参数是子字符串最后一个字符后面的位置
  3.	substr()===第二个参数是返回的字符个数
  4.	substring()===第二个参数是子字符串最后一个字符后面的位置
slice和substring的不同：在参数为负数时不同，slice和substr会将负数加上字符串长度返回，而substring会返回0.
（三）字符串位置方法：(都是从一个字符串中搜索给定的子字符串，返回子字符串的位置，没有返回-1)
1.	indexOf()===从字符串的开头向后搜索子字符串。
2.	lastIndexOf()===从字符串的末尾向前搜索子字符串。
（四）Trim方法：创建一个字符串的副本，删除前置和后缀的所有空格，返回结果
		trimLeft和trimRight方法
（五）字符串大小写转换方法：
1.	toLowerCase()===
2.	toLocaleCase()===
3.	toUpperCase()===
4.	toLocaleUpperCase()===
（六）字符串的模式匹配方法：
1.	match()===接受一个参数，一个正则表达式或者一个RegExp对象，返回一个数组。
2.	search()===参数同上，返回字符串第一个匹配项的索引，没有则返回-1.
（七）localeCpmpare方法：用于比较两个字符串，并返回（负数-1  or 0 or正数1）
（八）fromCharCode方法：接收一或多个字符编码，然后将他们转换成一个字符串。（感觉与charCodeAt方法执行相反的操作）。
